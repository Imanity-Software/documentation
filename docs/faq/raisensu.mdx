---
sidebar_position: 1
title: Licensing
description: General licensing question, claiming license, license details...
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Licensing System (Raisensu)

## Claiming your license

<Tabs groupId="platforms">
    <TabItem value="bbb" label="BuiltByBit (MC-Market)">
        <p>Claiming process are done by using <code>/claim</code> command in Discord.</p>
        <ol>
            <li>Join <a href={"https://go.imanity.dev/discord"}>Imanity Discord</a></li>
            <li>Go to channel <code>#🤖┃bot-commands</code></li>
            <li>Type <code>/claim</code></li>
            <li>Select the product you want to claim</li>
            <li>Enter your <b>BuiltByBit username</b></li>
            <li>Find your verify conversation in <a href={"https://builtbybit.com/conversations/"}>BuiltByBit
                Conversations page</a></li>
            <li>You should see a message contains your <code>/2factor</code> command in the BuiltByBit conversation</li>
            <li>Execute your <code>/2factor</code> command in <a
            href={"https://go.imanity.dev/discord"}>Imanity Discord</a></li>
            <li>Done! Now you can check your license info by using <code>!license</code> in <code>Raisensu#6662</code>'s private message.</li>
        </ol>
        <p>If you have any problems or difficult claiming your license, please open a ticket in <a
            href={"https://go.imanity.dev/discord"}>Imanity Discord</a>!</p>
    </TabItem>
    <TabItem value="tebex" label="Imanity Shop">
        <Admonition type="tip" icon="💡">
            <p>If you didn't disabled your private message in Discord privacy settings, you should already received an
                private message containing your purchase info from <code>Raisensu#6662</code> in Discord.</p>
            <p>All the purchase made from our <a href={"https://shop.imanity.dev"}>Imanity Shop</a> are automatically
                handled, that mean you don't need to manually claim any license.</p>
            <p>If you still have not receive your license within 1 hour, please open a ticket in <a
                href={"https://go.imanity.dev/discord"}>Imanity Discord</a>.</p>
        </Admonition>
    </TabItem>
</Tabs>

## What is IP Whitelists/Limit?

We will record the IP of your host/machine. For example, if you have **`2 IP Whitelists`** and you have two machines
with IPs `1.1.1.1` and `2.2.2.2`, this will count as <u>**2**</u> IPs.

Therefore, if you try to start the server on a host that
is `different from the IP in our database`, it will fail to start.

**You don’t need to add IP manually. Once you start the server, our system will automatically record the IP.**

:::note
It is based on **IP rather than the number of machines/hosts**. This means that if you have **multiple
machines** (for example, 5) running with the <u>**same IP**</u>, they will not be counted as 5 recorded IPs.
:::

---

## How to clear my IP records?

This can be done via using the discord command `/clear-ip-records`.

If you have multiple licenses, You can also use `/clear-ip-records <license>` to specify the license.

:::tip
This command has a restriction to prevent abuse. You can only clear IP records **once** every **48 hours**.
:::

## java.security.InvalidKeyException: Illegal key size

You are using any Java version before **`Java 8 u162`**, we highly suggest you update to the latest Java 8 possible.

If for any reason you need to use the current Java version, you will need to download [**JCE Unlimited Strength**][jce8] and
extract the jar files from the zip and save them in `${java.home}/jre/lib/security`

[jce8]: https://www.oracle.com/java/technologies/javase-jce8-downloads.html
